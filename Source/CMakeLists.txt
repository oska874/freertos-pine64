#configure compile
PROJECT(FreeRtos)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

SET(PORT portable)
SET(TARGET_EXE_Z "frpine")
#SET(FR_SOURCE_DIR ${PROJECT_SOURCE_DIR})
AUX_SOURCE_DIRECTORY(./src FR_SOURCE_DIR)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include ${PROJECT_SOURCE_DIR}/src/portable)
#INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src/portable/})
message(${PROJECT_SOURCE_DIR})
ADD_EXECUTABLE(${TARGET_EXE_Z} ${FR_SOURCE_DIR})

