SET(CMAKE_SYSTEM_NAME Generic)
SET(CMAKE_SYSTEM_PROCESSOR ARM)
SET(CMAKE_C_COMPILER "aarch64-elf-gcc")
SET(CMAKE_C_FLAGS_DEBUG " -fno-common -ffixed-x18 ")
SET(CMAKE_BUILD_TYPE "Debug")



PROJECT(FreeRtos)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 



SET(PORT portable)
SET(TARGET_EXE_Z "freepine")
SET(FR_SOURCE_DIR ${PROJECT_SOURCE_DIR})

INCLUDE_DIRECTORIES(${FR_SOURCE_DIR}/include)
INCLUDE_DIRECTORIES(${FR_SOURCE_DIR}/${PORT})

ADD_SUBDIRECTORY(${PORT})

TARGET_LINK_LIBRARIES(${TARGET_EXE_Z} libport)

